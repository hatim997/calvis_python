# clavis_event_inventory/suppliers/urls.py

from django.urls import path
from . import views # We will import actual views later

app_name = 'suppliers' # Sets the namespace for URLs in this app

urlpatterns = [
    # Add URL patterns for the suppliers app here later
    # Example: path('', views.supplier_list, name='list'),
]